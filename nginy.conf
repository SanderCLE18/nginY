server {
    httpListen 80;
    httpsListen 443;

    ssl_certificate     server.crt;
    ssl_certificate_key server.key;
    ssl_password_file   pass.key;

    #location /api {
    #    proxy_pass http://127.0.0.1:8080;
    #}
}